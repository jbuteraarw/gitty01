---
- hosts: all
  tasks:
  - name: ensure IIS web server is installed
    win_feature:
      name: Web-Server
      state: present
    when: ansible_os_family == "Windows"
  - name: Deploy htm file
    get_url:
      url: https://github.com/jbuteraarw/gitty01/blob/master/iisstart,j2
      dest: c:\inetpub\wwwroot\iisstart.htm